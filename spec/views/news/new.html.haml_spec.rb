require 'spec_helper'

describe "news/new" do

  before(:each) do
    pending 'news#new'
    # assign(:news, FactoryGirl.create(:news))
  end

  it "renders new news form" do
    pending 'news#new'
    # render
    # assert_select "form[action=?][method=?]", news_index_path, "post"
  end

end
