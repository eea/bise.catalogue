- content_for :pageheader do
  Editing Visualization

- content_for :actions do
  = link_to graphs_path, class: 'btn btn-default' do
    = fa_icon 'reply'
    Back
  = link_to @graph, class: 'btn btn-default' do
    = fa_icon 'search'
    Show

= render partial: 'graphs/modals'
= render 'form'
