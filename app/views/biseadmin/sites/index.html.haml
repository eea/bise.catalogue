- content_for :pageheader do
  Manage sites (Contributors)

- content_for :actions do
  - if current_user.approver?
    = link_to  new_biseadmin_site_path, class: "btn" do
      = fa_icon 'plus'
      New Site

.container-fluid
  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Description
        %th Access Token
        %th{ style: 'width: 75px' }
    %tbody
      - @sites.each do |site|
        %tr
          %td= site.name
          %td= raw site.description
          %td= site.auth_token

          %td
            .btn-group
              = link_to edit_biseadmin_site_path(site), class: 'btn btn-default btn-mini' do
                = fa_icon 'pencil'
                Edit
              = link_to biseadmin_site_path(site), method: :delete, class: 'btn btn-danger btn-mini', data: { confirm: 'Are you sure?' } do
                = fa_icon 'trash-o'
                Destroy
