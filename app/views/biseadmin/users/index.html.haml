- content_for :pageheader do
  Manage users

%table.table
  %thead
    %tr
      %th{ style: 'width: 30px' }
        \#
      %th
        User
      %th
        Sources
  %tbody
    - User.find_each do |u|
      %tr
        %td
          = u.id
        %td
          = link_to u.name, edit_user_registration_path(u)
        %td
          = "#{u.library_roles.size} Libraries"
