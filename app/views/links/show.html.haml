- content_for :pageheader do
  = @link.title

- content_for :actions do
  - if search_params_available?
    = link_to :back, class: 'btn btn-default' do
      = fa_icon 'reply'
      Back
  - else
    = link_to links_path, class: 'btn btn-default' do
      = fa_icon 'reply'
      Back to List
  - disabled = (can?(:update, @link)) ? 'disabled' : ''
  = link_to  edit_link_path(@link), class: 'btn btn-default', disabled: disabled do
    = fa_icon 'pencil'
    Edit
  = link_to  link_path(@link), method: :delete, class: 'btn btn-danger pull-right', disabled: disabled do
    = fa_icon 'trash-o'
    Delete



= render 'templates/common_info', obj: @link

%legend Link
.well
  = link_to @link.url, @link.url
  %br
  = raw @link.description

= render 'templates/countries', object: @link
= render 'templates/biographical_region', object: @link
= render 'templates/targets_and_actions', object: @link
= render 'templates/tags', object: @link
