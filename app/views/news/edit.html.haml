- content_for :pageheader do
  Editing a piece of news

- content_for :actions do
  = link_to news_index_path, :class => "btn" do
    %i.icon-reply
    Back
  = link_to @news, :class => "btn" do
    %i.icon-search
    Show

= render 'form'

